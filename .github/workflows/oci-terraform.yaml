name: OCI Terraform pipeline

on:
  pull_request:
    branches:
      - main
    paths:
      - 'oci/**'
  pull_request_target:
    types:
      - closed
    branches:
      - main
    paths:
      - 'oci/**'

  workflow_dispatch:
    inputs:
      env:
        description: "Environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  projects:
    name: 'projAzure Terraform pipeline'
    env:
      TF_ACTION_WORKSPACE: "default"
      SCENARIO: oci
      CORE_MODULE_VERSION: v0.0.17

    runs-on: self-hosted
    permissions:
      pull-requests: write
      actions: read
      contents: read
      packages: read
      repository-projects: read

    steps:
    - name: 'Checkout'
      uses: actions/checkout@v4

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Configure Git credentials for private repos
      run: |
        git config --global url."https://${{ secrets.GH_PAT }}@github.com/".insteadOf "https://github.com/"

    - name: Clone OCI Orchestrator
      env:
        GH_TOKEN: ${{ secrets.GH_PAT }}
      run: git clone https://${GH_TOKEN}@github.com/iblake/clickops-terraform-oci-modules-orchestrator.git ORCH

    - name: 'Setup Terraform'
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.12.1
        terraform_wrapper: true

    - name: 'Setup Orchestrator Instance Principal Auth'
      id: backend
      run: |
        cat > ${{ github.workspace }}/ORCH/providers.tf << 'EOF'
        provider "oci" {
          auth                = "InstancePrincipal"
          region              = var.region
          ignore_defined_tags = ["Oracle-Tags.CreatedBy", "Oracle-Tags.CreatedOn"]
        }

        provider "oci" {
          auth                = "InstancePrincipal"
          alias               = "home"
          region              = var.region
          ignore_defined_tags = ["Oracle-Tags.CreatedBy", "Oracle-Tags.CreatedOn"]
        }

        provider "oci" {
          auth                = "InstancePrincipal"
          alias               = "secondary_region"
          region              = var.region
          ignore_defined_tags = ["Oracle-Tags.CreatedBy", "Oracle-Tags.CreatedOn"]
        }

        terraform {
          required_version = ">= 1.3.0"
          required_providers {
            oci = {
              source                = "oracle/oci"
              configuration_aliases = [oci.home]
            }
          }
          backend "oci" {
            bucket    = "__BUCKET__"
            namespace = "__NAMESPACE__"
            auth      = "InstancePrincipal"
            region    = "__REGION__"
          }
        }
        EOF
        source /opt/actions-runner/.github-runner-env
        sed -i "s|__BUCKET__|$OCI_TF_STATE_BUCKET|g" ${{ github.workspace }}/ORCH/providers.tf
        sed -i "s|__NAMESPACE__|$OCI_TF_STATE_NAMESPACE|g" ${{ github.workspace }}/ORCH/providers.tf
        sed -i "s|__REGION__|$OCI_REGION|g" ${{ github.workspace }}/ORCH/providers.tf
        cd ${{ github.workspace }}/ORCH
        terraform fmt providers.tf

    - name: 'Terraform Init'
      id: init
      run: |
        cd ${{ github.workspace }}/ORCH
        terraform init

    - name: Replace sensitive values in config files
      run: |
        source /opt/actions-runner/.github-runner-env
        find ${{ github.workspace }}/oci -name '*.json' -exec sed -i "s|__ATP_ADMIN_PASSWORD__|$ATP_ADMIN_PASSWORD|g" {} \;

    - name: Terraform Format
      id: format
      if: github.event_name == 'pull_request'
      run: |
        cd ${{ github.workspace }}/ORCH
        terraform fmt
        terraform fmt -check

    - name: 'Terraform Validate'
      id: validate
      if: github.event_name == 'pull_request'
      run: |
        cd ${{ github.workspace }}/ORCH
        terraform validate -no-color

    - name: 'Terraform Plan'
      id: plan
      if: github.event_name == 'pull_request'
      run: |
        source /opt/actions-runner/.github-runner-env
        cd ${{ github.workspace }}/ORCH
        SCENARIO_DIR=${{ github.workspace }}/$SCENARIO
        VARS=$(find $SCENARIO_DIR -name '*.json')
        echo "$VARS"
        terraform plan -detailed-exitcode -no-color \
          -var="region=${OCI_REGION}" \
          -var="tenancy_ocid=dummy" \
          -var="user_ocid=dummy" \
          -var="fingerprint=dummy" \
          -var="private_key_path=dummy" \
          -var="private_key_password=dummy" \
          $(for i in $VARS; do echo "-var-file $i"; done) -out=tfplan.binary
        echo "plan-exit-code=$?" >> $GITHUB_OUTPUT

    - name: Post PR comment
      if: github.event_name == 'pull_request'
      uses: actions/github-script@v7
      env:
        PLAN: ${{ steps.plan.outputs.stdout }}
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }}
        script: |
          const output = `#### Terraform Format 🖌 \`${{ steps.format.outcome }}\`
          #### Terraform Validate 🤖 \`${{ steps.validate.outcome }}\`
          #### Terraform Plan 📖 \`${{ steps.plan.outcome }}\`

          <details><summary>Show Plan</summary>

          \`\`\`terraform
          ${process.env.PLAN}
          \`\`\`

          </details>

          *Pushed by: @${{ github.actor }}*`;
          github.rest.issues.createComment({
            issue_number: context.issue.number,
            owner: context.repo.owner,
            repo: context.repo.repo,
            body: output
          });

    - name: Notify Slack on Plan success
      if: success() && github.event_name == 'pull_request'
      run: |
        curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
          -H 'Content-Type: application/json' \
          -d '{
            "text": "✅ OCI Terraform plan succeeded on PR #${{ github.event.number }}",
            "blocks": [{
              "type": "section",
              "text": {
                "type": "mrkdwn",
                "text": "*✅ OCI Terraform Plan Succeeded*\n*PR:* <${{ github.event.pull_request.html_url }}|#${{ github.event.number }} - ${{ github.event.pull_request.title }}>\n*Author:* @${{ github.actor }}\n*Status:* Init ✓ | Validate ✓ | Plan ✓"
              }
            }]
          }'

    - name: Notify Slack on failure
      if: failure()
      run: |
        curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
          -H 'Content-Type: application/json' \
          -d '{
            "text": "❌ OCI Terraform workflow FAILED",
            "blocks": [{
              "type": "section",
              "text": {
                "type": "mrkdwn",
                "text": "*❌ OCI Terraform Workflow FAILED*\n*PR:* <${{ github.event.pull_request.html_url }}|#${{ github.event.number }}>\n*Logs:* <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View workflow logs>\n*Author:* @${{ github.actor }}"
              }
            }]
          }'

    - name: Terraform Apply (on PR merge)
      id: apply
      if: >
        github.event_name == 'pull_request_target' &&
        github.event.action == 'closed' &&
        github.event.pull_request.merged == true
      run: |
        source /opt/actions-runner/.github-runner-env
        cd ${{ github.workspace }}/ORCH
        SCENARIO_DIR=${{ github.workspace }}/$SCENARIO
        VARS=$(find $SCENARIO_DIR -name '*.json')
        terraform apply -auto-approve \
          -var="region=${OCI_REGION}" \
          -var="tenancy_ocid=dummy" \
          -var="user_ocid=dummy" \
          -var="fingerprint=dummy" \
          -var="private_key_path=dummy" \
          -var="private_key_password=dummy" \
          $(for i in $VARS; do echo "-var-file $i"; done)

    - name: Get deployed resources info
      id: resources
      if: >
        github.event_name == 'pull_request_target' &&
        github.event.action == 'closed' &&
        github.event.pull_request.merged == true &&
        success()
      run: |
        cd ${{ github.workspace }}/ORCH

        # Get Autonomous Databases from terraform state
        ADBS=$(terraform show -json 2>/dev/null | jq -r '
          .values.root_module.child_modules[]? |
          select(.address | contains("oci_lz_autonomous_databases")) |
          .resources[]? |
          select(.type == "oci_database_autonomous_database") |
          "• " + .values.display_name + " [" + .values.db_workload + "]\n  Connection: " + .values.connection_strings[0].profiles[0].value
        ' 2>/dev/null)

        # Get VCNs from terraform state
        VCNS=$(terraform show -json 2>/dev/null | jq -r '
          .values.root_module.child_modules[]? |
          .resources[]? |
          select(.type == "oci_core_vcn") |
          "• " + .values.display_name + " (" + .values.cidr_blocks[0] + ")"
        ' 2>/dev/null)

        # Get Compute instances from terraform state
        COMPUTE=$(terraform show -json 2>/dev/null | jq -r '
          .values.root_module.child_modules[]? |
          .resources[]? |
          select(.type == "oci_core_instance") |
          "• " + .values.display_name
        ' 2>/dev/null)

        # Get Public IPs from terraform outputs
        PUBLIC_IPS=$(terraform output -json 2>/dev/null | jq -r '
          to_entries[] |
          select(.key | contains("public_ip") or contains("ip_address")) |
          "• " + .value.value
        ' 2>/dev/null)

        echo "resources<<EOF" >> $GITHUB_OUTPUT
        if [ -n "$VCNS" ]; then
          echo "VCNs:" >> $GITHUB_OUTPUT
          echo "$VCNS" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
        fi
        if [ -n "$COMPUTE" ]; then
          echo "Compute Instances:" >> $GITHUB_OUTPUT
          echo "$COMPUTE" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
        fi
        if [ -n "$ADBS" ]; then
          echo "Autonomous Databases:" >> $GITHUB_OUTPUT
          echo "$ADBS" >> $GITHUB_OUTPUT
          echo "" >> $GITHUB_OUTPUT
        fi
        if [ -n "$PUBLIC_IPS" ]; then
          echo "Public IPs:" >> $GITHUB_OUTPUT
          echo "$PUBLIC_IPS" >> $GITHUB_OUTPUT
        fi
        echo "EOF" >> $GITHUB_OUTPUT

    - name: Notify Slack on Apply success
      if: >
        github.event_name == 'pull_request_target' &&
        github.event.action == 'closed' &&
        github.event.pull_request.merged == true &&
        success()
      run: |
        RESOURCES="${{ steps.resources.outputs.resources }}"
        source /opt/actions-runner/.github-runner-env
        curl -X POST ${{ secrets.SLACK_WEBHOOK_URL }} \
          -H 'Content-Type: application/json' \
          -d "{
            \"text\": \"✅ OCI Infrastructure Deployed Successfully\",
            \"blocks\": [{
              \"type\": \"section\",
              \"text\": {
                \"type\": \"mrkdwn\",
                \"text\": \"*✅ OCI Infrastructure Deployed*\n*PR:* <${{ github.event.pull_request.html_url }}|#${{ github.event.number }} - ${{ github.event.pull_request.title }}>\n*Author:* @${{ github.actor }}\n*Region:* ${OCI_REGION}\n\n*Deployed Resources:*\n\`\`\`\n${RESOURCES}\n\`\`\`\"
              }
            }, {
              \"type\": \"context\",
              \"elements\": [{
                \"type\": \"mrkdwn\",
                \"text\": \"<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View deployment logs>\"
              }]
            }]
          }"
